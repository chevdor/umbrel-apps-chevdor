version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: polkadot-delegation-dashboard
      APP_PORT: 80

  delegation-dashboard:
    container_name: polkadot-delegation-dashboard
    image: docker.io/chevdor/polkadot-delegation-dashboard
